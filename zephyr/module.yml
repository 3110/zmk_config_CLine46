name: CLine46
build:
  # src/ のコードを足すために cmake と kconfig を宣言している
  # （board_root だけだった頃は shield の定義しか持っていなかった）
  cmake: .
  kconfig: Kconfig
  settings:
    board_root: .
    # dts/behaviors（&status_adv の定義）と dts/bindings、
    # include/dt-bindings（SADV_* の定義）をキーマップから見えるようにする
    dts_root: .
